import 'dart:io';

import 'package:flutter/material.dart';

class AdaptiveTextFialet extends StatelessWidget {
  // final TextEditingController _label;
  // final double _amount;

  // const AdaptiveTextFialet({
  //   Key? key,
  // }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
    // Platform.isAndroid
    // ? TextField(
    //     decoration: InputDecoration(labelText: "Harajat nomi"),
    //     controller: titleController,
    //   )
    // : CupertinoTextField(
    //     placeholder: 'Harajat nomi',
    //     controller: titleController,
    //   );
  }
}
